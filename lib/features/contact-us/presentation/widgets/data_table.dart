import 'package:flutter/material.dart';

class DashboardContactUsTable extends StatelessWidget {
  const DashboardContactUsTable({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
